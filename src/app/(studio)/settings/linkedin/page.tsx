import Link from "next/link";
import { CheckCircle2, ExternalLink, Link2Off } from "lucide-react";
import { getCurrentUser } from "@/lib/auth";
import { db } from "@/lib/db";
import { env } from "@/lib/env";
import { DisconnectLinkedIn } from "@/components/DisconnectLinkedIn";

export default async function LinkedInSettingsPage({ searchParams }: { searchParams: Promise<{ connected?: string; error?: string }> }) { const user = await getCurrentUser(); if (!user) return null; const params = await searchParams; const credential = await db.oAuthCredential.findUnique({ where: { userId_provider: { userId: user.id, provider: "linkedin" } }, select: { memberName: true, memberUrn: true, expiresAt: true } }); return <main className="page"><div className="page-header"><div><div className="eyebrow">Permissioned publishing</div><h1 className="page-title">LinkedIn connection</h1><p className="page-description">Connect only the personal profile you intend to publish to. SMCS never automates browser clicks or stores credentials in n8n.</p></div><Link className="btn btn-secondary" href="/settings">Back to settings</Link></div>{params.error && <div className="notice notice-error" style={{ marginBottom: 18 }}>LinkedIn connection could not be completed. Check your app callback and permissions.</div>}{params.connected && <div className="notice notice-info" style={{ marginBottom: 18 }}>LinkedIn profile connected successfully.</div>}<div className="grid two-col"><section className="card"><h2 className="card-title">Personal profile</h2>{credential ? <><div className="notice notice-info" style={{ marginTop: 16, display: "flex", gap: 9, alignItems: "center" }}><CheckCircle2 size={18} />Connected as <strong>{credential.memberName || credential.memberUrn}</strong></div><p className="helper">Token expiry: {credential.expiresAt ? credential.expiresAt.toLocaleString() : "Provider-managed"}</p><DisconnectLinkedIn /></> : <><div className="notice notice-warn" style={{ marginTop: 16 }}>Not connected. Live mode cannot publish until OAuth is complete.</div><a className="btn btn-primary" style={{ marginTop: 16 }} href="/api/linkedin/connect"><Link2Off size={15} />Connect LinkedIn</a></>}</section><section className="card"><h2 className="card-title">Required configuration</h2><ul className="helper" style={{ lineHeight: 1.8, paddingLeft: 18 }}><li>LinkedIn developer app with Sign In with LinkedIn and Share on LinkedIn.</li><li>Redirect URI: <code>{env.LINKEDIN_REDIRECT_URI}</code></li><li>Member posting permission: <code>w_member_social</code></li><li>Server-only client secret and token encryption key.</li></ul><a className="helper" href="https://learn.microsoft.com/en-us/linkedin/marketing/community-management/shares/posts-api" target="_blank" rel="noreferrer">Read the official Posts API documentation <ExternalLink size={13} style={{ verticalAlign: "middle" }} /></a></section></div></main>; }
