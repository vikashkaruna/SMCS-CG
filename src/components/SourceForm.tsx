"use client";
import { useState } from "react";
async function csrf() { const response = await fetch("/api/auth/csrf"); return (await response.json()).token as string; }
export function SourceForm() { const [message, setMessage] = useState(""); const [error, setError] = useState(""); async function submit(form: FormData) { const response = await fetch("/api/sources", { method: "POST", headers: { "Content-Type": "application/json", "x-csrf-token": await csrf() }, body: JSON.stringify(Object.fromEntries(form)) }); const data = await response.json(); if (response.ok) { setMessage("Source added. Refresh to see it in the list."); (document.querySelector("form") as HTMLFormElement)?.reset(); } else setError(data.error || "Unable to add source."); } return <form action={submit} className="form-grid" style={{ marginTop: 17 }}><div className="field"><label htmlFor="name">Source name</label><input className="input" id="name" name="name" required placeholder="Research publication" /></div><div className="field"><label htmlFor="url">Source URL</label><input className="input" id="url" name="url" type="url" required placeholder="https://…" /></div><div className="field"><label htmlFor="tier">Tier</label><select className="select" id="tier" name="tier"><option value="PRIMARY">Primary</option><option value="CREDIBLE">Credible reporting</option><option value="DISCOVERY">Discovery cue</option></select></div>{message && <div className="notice notice-info">{message}</div>}{error && <div className="notice notice-error">{error}</div>}<button className="btn btn-primary">Add source</button></form>; }
